# Credentials - never commit
id.txt
id_2.txt
clientID.txt
clientSecret.txt
default_credentials.json

# Personal Claude Code settings (permissions etc.) - not shared with the team
.claude/settings.local.json

# Dev-only project notes for Claude Code - not meant to be public
CLAUDE.md

# Kevin's personal local-dev launcher (assumes a "venv" folder next to it)
Launch Track Tidy.bat

# Python
__pycache__/
*.pyc
venv/
venv_build/

# Build artifacts
build/
dist/
installer_output/

# Large binaries / bundled tools
ffmpeg.exe
fpcalc.exe

# Audio test/sample files (but NOT the two app assets used as UI sound
# effects - see resource_path() in interface.py - and bundled by PyInstaller)
*.mp3
*.wav
!fart.wav
!success.wav
