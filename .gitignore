# Credentials - never commit
id.txt
clientID.txt
clientSecret.txt

# Python
__pycache__/
*.pyc
venv/
venv_build/

# Build artifacts
build/
dist/
installer_output/

# Large binaries / bundled tools
ffmpeg.exe

# Audio test/sample files (but NOT the two app assets used as UI sound
# effects - see resource_path() in interface.py - and bundled by PyInstaller)
*.mp3
*.wav
!fart.wav
!success.wav
